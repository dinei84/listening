# PROJECT_STATE.md

Fonte única de verdade sobre o estado atual do projeto. Deve ser atualizado a cada OS concluída — quem faz a atualização é o próprio agente que finalizou a OS, como último passo antes de abrir o PR.

Se um agente novo entra no projeto, este é o primeiro arquivo que ele lê, antes de `ARQUITETURA.md` e `AGENTS.md`.

---

## 1. Visão em uma linha

App pessoal que converte PDF em audiobook (estilo Audible), com pipeline plugável de extração de texto/OCR e TTS, otimizado para baixo custo.

## 2. Status atual

**Fase:** os três níveis do produto (decisão #23) estão implementados **na arquitetura**. **Simples** (padrão de hoje): extração local + Kokoro, custo zero, nenhuma rede — `config.yaml` com `speaker: kokoro` e `normalizer: noop`. **Médio** (OS-038): `TextNormalizer` com LLM via endpoint compatível com OpenAI, opt-in por livro, ~US$ 0,16–0,87/livro. **Premium**: terreno pronto (OS-042 trava de custo, OS-043 limite por engine + retry), mas **falta escolher o motor de TTS** — bloqueado na OS-041 por credencial. **O que NÃO existe ainda é a camada de produto**: não há conceito de usuário, conta ou plano; o nível é escolhido por `config.yaml` (global) + flag por livro.

**Última OS concluída:** OS-038 — ajudante LLM (nível médio), com o opt-in na tela do player.

**OS em andamento:** nenhuma.

**Próxima OS a abrir:** **camada de produto / multitenancy** (ver seção 8, ainda a redigir como OS) — é o próximo passo declarado pelo dono. Em paralelo, o premium segue bloqueado na OS-041 (credencial de TTS).

**Achados de teste real (sessão de 2026-08-05, investigados e confirmados) — todos endereçados pela OS-033, exceto onde indicado:** (a) `DELETE /books/{id}` bloqueia (409) livros em `uploaded`, que estão só enfileirados sem nada sendo escrito — a justificativa da decisão #17 não se aplica a esse status, e na prática nenhum livro era deletável enquanto um livro grande sintetizava; agravado pelo player descartar o motivo do erro (`player/app.js` troca o `detail` da API por "Falha ao deletar o livro"). Resolvido pela OS-033 (decisão #22): `uploaded` saiu de `_BLOCKED_DELETE_STATUSES` e o player passou a mostrar o `detail` real. (b) Clicar num livro da lista não preenche o campo "Abrir livro existente" (`player/app.js` chama `openBook()` sem setar `bookIdInput.value`) — nunca preencheu, não é regressão; resolvido pela OS-033 (seção 2.2). (c) Livro `queued` aparece na UI como `uploaded` cru, sem dizer que está esperando outro livro terminar — foi o que fez parecer que o sistema estava travado. Resolvido pela OS-033 (`statusLabel` → "Na fila — aguardando processamento").

## 3. Decisões já tomadas (Architecture Decision Log)

Registrar aqui toda decisão relevante, na ordem em que foram tomadas. Nunca apagar uma entrada — se uma decisão for revertida, adicionar uma nova entrada explicando o motivo, mantendo o histórico.

| # | Data | Decisão | Motivo |
|---|------|---------|--------|
| 1 | definir na 1ª OS | Arquitetura plugável para Extractor e Speaker via interface + registry | Permite trocar OCR/TTS sem reescrever pipeline; ver `ARQUITETURA.md` seção 1 |
| 2 | definir na 1ª OS | TTS local (Kokoro) como padrão, cloud como opção sob demanda | Controle de custo — ver brainstorm original |
| 3 | em aberto | Fila de jobs: Celery+Redis vs solução mais simples (SQLite como fila) | Pendente — depende do volume de uso real (projeto pessoal, não precisa de infra pesada) |
| 4 | em aberto | Banco de dados: SQLite (MVP) com migração futura para Postgres | Pendente confirmação |
| 5 | em aberto | Heurística de fallback de OCR (quando cair de Tesseract → PaddleOCR → cloud) | Precisa de uma OS dedicada de spike/pesquisa |
| 6 | 2026-08-03 | Atualizações de governança (`AGENTS.md`, `README.md`, `TEMPLATE.md` etc.) feitas aqui no repositório de arquitetura precisam ser baixadas e commitadas manualmente no repositório de código — não há sincronização automática | Descoberto durante a OS-001: mudanças ficaram como "não commitadas" no repo de código e quase foram atribuídas erroneamente a um agente de execução. Toda vez que a documentação de governança for atualizada aqui, o próximo agente deve confirmar via `git diff` contra o último commit se os docs já estão sincronizados antes de assumir que uma mudança veio de execução indevida |
| 7 | 2026-08-03 | Estrutura de documentação do repositório de código usa `docs/os/` e `docs/report/` em minúsculo (não `docs/OS/`/`docs/REPORT/`) | Encontrada divergência de caixa entre o que a governança definia e o que existia no repo de código; corrigido por renomeação para bater com a convenção documentada em `README.md` |
| 8 | 2026-08-03 | Proposta (OS-005): considerar OCR "confiança baixa" quando `avg_confidence_words_normalized < 0.85` **ou** `words_counted == 0`; nesses casos, cair para o próximo extractor da cadeia (`TesseractOCR` → `PaddleOCR` → `CloudOCRFallback`) | Spike empírico com Tesseract 5.3.4 em 4 fixtures mostrou cenários de alta confiança (~0.90-0.96) e falha total (`words_counted == 0`, confiança 0.0) em imagem muito degradada. PaddleOCR confidence pesquisado em documentação oficial (`rec_score` / `rec_scores`) mas **não validado empiricamente** neste ambiente |
| 9 | 2026-08-03 | **Decisão #8 aprovada pelo dono do projeto, sem alterações.** A heurística `avg_confidence_words_normalized < 0.85` ou `words_counted == 0` é agora oficial e foi incorporada em `ARQUITETURA.md` seção 4.1. Isso resolve a decisão #5 | Aprovação explícita em conversa, após revisão que confirmou a reprodutibilidade dos números do spike. Ressalva conhecida (não invalida a aprovação): as fixtures usadas cobriram bem sucesso (~0.90-0.96) e falha total (0.0), mas não um caso de degradação intermediária — `0.85` é uma margem de segurança, não um ponto fino validado empiricamente |
| 10 | 2026-08-03 | **Decisão #4 confirmada: SQLite para a API mínima (OS-010), processamento síncrono no request (sem fila).** A decisão #3 (Celery/Redis vs. algo mais simples) continua em aberto — só passa a importar quando a API precisar mesmo ser assíncrona | Confirmado pelo dono do projeto. Segue o roadmap de `ARQUITETURA.md` seção 8, que já colocava a API (passo 2) antes da fila assíncrona (passo 4). Evita resolver a decisão #3 antes da hora, mantendo a filosofia de baixa infraestrutura do `HANDOFF.md` |
| 11 | 2026-08-03 | **Decisão #3 resolvida: fila de jobs em SQLite (`SQLiteJobQueue`), tratada como plugin.** Contrato `JobQueue` novo, proposto e aprovado pelo dono do projeto, incorporado em `ARQUITETURA.md` seção 4.3. Celery+Redis fica descartado por agora, mas a interface já existe para que uma futura `RedisJobQueue` seja só uma nova classe + entrada no registry, sem reescrever `core/pipeline.py`/`api/`/`worker/tasks.py` | Segue a mesma regra de ouro de plugin já usada para Extractor/Speaker (`ARQUITETURA.md` seção 1: "pode ser substituído por algo melhor no futuro, é plugin"). Dono do projeto pediu explicitamente para não fechar a porta pra Redis mais tarde, sem adicionar a complexidade dele agora |
| 12 | 2026-08-04 | **Player web em HTML/CSS/JS puro, sem build step (sem React).** `ARQUITETURA.md` seção 3 tinha "React" só como comentário informal na árvore de pastas original — nunca foi decisão formal. Atualizado para refletir a escolha real | Confirmado pelo dono do projeto. Mesma filosofia de baixa infraestrutura já aplicada o projeto inteiro (sem ORM, sem Celery, stdlib sempre que dá) — React exigiria npm/node_modules/bundler, uma segunda toolchain só pro player "básico" |
| 13 | 2026-08-04 | **Terceiro extractor da cadeia de OCR é `EasyOCRExtractor` (torch), não `PaddleOCR` como no roadmap original.** `paddlepaddle` sozinho baixa ~195MB (framework de deep learning novo); `torch` já está instalado via Kokoro, e `EasyOCR` o usa em vez de trazer um segundo framework do zero. `ARQUITETURA.md` seções 3, 4.1, 4.4 e 8 atualizadas (`paddle_ocr.py`→`easyocr_extractor.py`, `"paddleocr"`→`"easyocr"` no registry). Fórmula de confidence: mesma faixa 0.0–1.0 do PaddleOCR original, então reaproveita o threshold `0.85` (decisão #9) por analogia ao Tesseract — **não é uma nova validação empírica**, é uma decisão de engenharia justificada pela semelhança de formato do confidence, a confirmar/ajustar quando `EasyOCRExtractor` tiver uso real | Confirmado pelo dono do projeto. Mesma filosofia de baixa infraestrutura (decisão #12, `HANDOFF.md` seção 2) — evitar uma segunda dependência pesada de deep learning quando a primeira (torch, via Kokoro) já cobre a mesma necessidade |
| 14 | 2026-08-04 | **Bug de correção encontrado (não uma decisão de arquitetura, registrado aqui por ser institucionalmente importante): `KokoroSpeaker` usava a API errada do Kokoro desde a OS-004.** `pipeline.generate_from_tokens(text, ...)` recebendo uma `string` trata ela como se já fosse fonemas prontos (IPA), pulando o G2P (grafema→fonema) por completo — confirmado comparando `"The quick brown fox..."` (texto bruto usado) com `'ðə kwˈɪk bɹˈWn fˈɑks...'` (G2P real do Kokoro pro mesmo texto). Corrigido na OS-019: trocar para `pipeline(text, voice=..., speed=...)` (`__call__`), que faz G2P de verdade **e** já divide texto longo respeitando o limite de fonemas internamente — testado com 1350 caracteres, devolveu 3 pedaços de forma automática e correta. Isso também tornou a lógica de divisão/retry manual da OS-018 desnecessária (removida na OS-019) | Descoberto em revisão manual antes de mergear a OS-018 — os testes automatizados nunca detectaram isso porque só verificam propriedades estruturais do áudio (arquivo existe, duração > 0), nunca o conteúdo fonético/pronúncia. Lição institucional: para engines de terceiros com múltiplos métodos parecidos (`generate_from_tokens` vs. `__call__`), ler a documentação/docstring de cada um antes de escolher, não assumir pelo nome mais direto |

| 15 | 2026-08-05 | **Contrato `JobQueue` estendido com `requeue_orphaned() -> list[Job]` (OS-022), extensão aditiva do contrato original da OS-011** — nada removido ou alterado, mas toda implementação de `JobQueue` precisa passar a tê-lo. Incorporado em `ARQUITETURA.md` seção 4.3. Limitação aceita e documentada: o método assume **um único worker ativo por vez** — não existe heartbeat/lease, então qualquer `Job` em `running` encontrado na inicialização do worker é tratado como órfão; com dois workers simultâneos, o segundo a iniciar devolveria para a fila o `Job` que o primeiro está processando de verdade | Sem isso, um `Job` interrompido no meio (worker morto/Ctrl+C) fica invisível pra sempre — `claim_next()` só enxerga `queued` — e todo o trabalho já feito se perde. Heartbeat/lease resolveria o caso multi-worker, mas é complexidade de infraestrutura que o projeto (um worker só, decisão #11 e filosofia do `HANDOFF.md`) não justifica hoje |
| 16 | 2026-08-05 | **Caminho seguro escolhido para inconsistência na retomada (OS-022): marcar `Book.status = "error"` com mensagem explícita pedindo reenvio, sem apagar os `AudioChunk`s já persistidos.** A alternativa oferecida pela OS era descartar os chunks e reprocessar do zero automaticamente. Detecção: existe chunk persistido com `sequence >= ` número de chunks do texto re-extraído | Duas razões: (1) apagar áudio já gerado é destrutivo e irreversível — o custo de errar é maior que o de pedir um reenvio manual, e `storage/audio_store.py` não tem função de remoção (criar uma ampliaria o escopo da OS); (2) uma inconsistência aqui significa que o PDF ou a lógica de limpeza/chunking mudou entre as duas tentativas, algo que merece atenção humana e não um reprocessamento silencioso |
| 17 | 2026-08-05 | **`DELETE /books/{id}` bloqueia a exclusão (409) enquanto o livro está em `uploaded`/`extracting`/`processing`/`synthesizing`** — status em que o worker ainda pode estar ativo. Só `ready`/`error` são deletáveis | Evita remover um arquivo/diretório que o worker ainda está escrevendo — `audio_store.persist_chunks()` usa `shutil.move` e apagar o destino no meio corromperia o chunk atual e os seguintes — e evita o worker chamar `update_book_status()` num `book_id` que não existe mais. Cancelar o `Job` em andamento em vez de bloquear foi considerado e deixado fora de escopo (OS-023 seção 2) |
| 18 | 2026-08-05 | **Progresso da síntese (`chunks_done`/`chunks_total`) entra no response existente de `GET /books/{id}/status`, sem endpoint novo; `Book.chunk_total` gravado no banco por `process_job()` antes de marcar `synthesizing`** | O player já faz polling nesse endpoint para cada livro — ampliar o response evita um endpoint/request extra por poll e mantém um único caminho de status. Gravar o total no banco (coluna nova `chunk_total`, `storage/db.py::set_book_chunk_total()`) em vez de só calcular em memória faz o total sobreviver a worker interrompido/retomada (OS-022), e reusa o `chunk_count = count_text_chunks()` que a checagem de consistência já calculava. `chunks_total` é `null` até a síntese começar, e a UI mostra o texto cru de status como fallback nesse caso |
| 19 | 2026-08-05 | **PROPOSTA do spike da OS-031 (pendente de aprovação do dono — APROVADA, ver decisão #20): não mudar nada na síntese.** Batching via `KPipeline.__call__(List[str])`, tamanho de chunk maior e redução do overhead de entorno **não** trazem ganho mensurável: batching ≈ +1% (ruído), throughput constante (~63× áudio/tempo) para `max_chars` 1000/1500/2000/3000, G2P + escrita do `.wav` ≈ 3% do tempo por chunk, GPU saturada (~89% média). O gargalo é a inferência pura | Medido com script reproduzível (`scripts/spike_synthesis_performance.py`, corpus fixo de 19.204 caracteres, RTX 3060 CUDA). Toda a análise e os trade-offs em `docs/report/OS-031-report.md`. Se aprovada, nenhuma OS de implementação é necessária; se rejeitada ou se a performance um dia importar, as alavancas apontadas são modelo mais leve/GPU maior — fora do escopo do spike |
| 20 | 2026-08-05 | **Decisão #19 aprovada pelo dono do projeto, sem alterações: não mudar nada na síntese "por enquanto".** Nenhuma OS de implementação de performance será aberta; `KokoroSpeaker`, `chunk_text()` (`DEFAULT_MAX_CHARS = 1000`) e o fluxo de um chunk por chamada ficam como estão. O "por enquanto" é parte da decisão: ela vale para o hardware e o modelo atuais (RTX 3060, Kokoro-82M) e deve ser revisitada se qualquer um dos dois mudar, ou se o tempo de síntese voltar a incomodar em uso real | Aprovação explícita em conversa, após revisão que confirmou a causa raiz no código do Kokoro: `KPipeline.__call__` chama `KPipeline.infer()` uma vez por segmento de fonemas dentro do loop (`kokoro/pipeline.py` linhas 32 e 80) — não existe batch real na GPU, o que explica causalmente o +1% medido, não só empiricamente. Aumentar `max_chars` seria prejuízo puro: não acelera nada (custo proporcional ao áudio gerado, não ao nº de chamadas) e pioraria a granularidade do playback incremental (OS-021/030) e da retomada (OS-022) |

| 21 | 2026-08-05 | **Preempção de fila é cooperativa, não preemptiva de verdade (OS-032).** O worker não mata processo nem interrompe thread: entre um chunk e outro, no `on_chunk` que já existia desde a OS-021, ele pergunta `queue.should_yield(job.id)` e, se `True`, para no fim do chunk corrente (~1s de granularidade, nunca no meio — interromper no meio desperdiçaria o trecho) e devolve o próprio `Job` para `queued` via `queue.requeue(job.id)` preservando a prioridade, marcando o `Book` como `paused`. A preempção **não apaga nada** — os `AudioChunk`s já persistidos (OS-021) é que permitem retomar de onde parou (OS-022). Decisão de implementação registrada junto: `POST /books/{id}/prioritize` responde **409** para livro `ready` **ou** `error` (não há o que processar; priorizar um `Job` `failed` seria no-op), **404** se o livro ou o `Job` dele não existir, e 200 nos demais casos (incluindo `paused`, cujo `Job` já está `queued` — priorizar apenas o coloca no topo) | Mantém a filosofia de baixa infraestrutura do projeto (decisão #11 e `HANDOFF.md`): nenhuma infra de interrupção/lease nova, só uma consulta barata por chunk. O custo é até ~1s de latência para a troca, aceito e documentado na OS-032. Um livro `paused` não é `error` nem `ready`: o polling do player para (não há chunk novo vindo), mas o áudio já sintetizado continua tocável — mesmo espírito do tratamento de `error` da OS-030, e `paused` **não** entra em `_BLOCKED_DELETE_STATUSES` (decisão #17): num livro pausado ninguém está escrevendo, é deletável |
| 22 | 2026-08-05 | **Decisão #17 é alterada (não revogada): `DELETE /books/{id}` deixa de bloquear o status `uploaded` (OS-033).** `_BLOCKED_DELETE_STATUSES` passa a ser `{"extracting", "processing", "synthesizing"}`. O princípio original ("não apagar o que o worker pode estar escrevendo") continua valendo — o que mudou é o reconhecimento de que um livro `uploaded` tem o `Job` apenas `queued`, o worker não o tocou e nada está sendo escrito, então a justificativa da #17 não se aplica a esse status. **Corrida conhecida e considerada benigna, não re-arquitetar:** existe uma janela estreita em que o worker chama `claim_next()` no exato momento da exclusão; já é seguro hoje, porque `process_job()` faz `book = db.get_book(job.book_id)` e, se `None`, chama `mark_failed()` e retorna sem tocar em disco. Na prática (achado que motivou a OS-033), com um livro grande sintetizando por horas, nenhum livro era deletável — o bloqueio a `uploaded` não protegia nada de real. De brinde na mesma OS: `GET /books/{id}/status` passou a devolver `title` (mudança aditiva, player mostra título em vez de UUID) e a UI traduz o status cru `uploaded` para "Na fila — aguardando processamento" só na exibição, sem mexer em `Book.status` | Confirmado pelo dono do projeto ao escrever a OS-033. Registrado como entrada nova, sem editar a #17 retroativamente (seção 7) |
| 23 | 2026-08-05 | **O MVP passa a ser desenhado como produto em três níveis, e isso define onde cada melhoria entra.** **Simples** (o que já existe): extração local + Kokoro local, zero custo variável, **nenhuma IA** — só processamento determinístico. **Médio**: soma uma LLM barata para normalizar texto e classificar trechos (a OS-038, reposicionada de "opt-in por livro" para "nível do plano"). **Premium**: troca o motor de TTS por um `Speaker` cloud com voz humana, mais a IA do nível médio. **Consequência imediata registrada aqui:** toda melhoria que puder ser feita de forma determinística e sem custo pertence ao nível **simples**, não ao pago — se `**negrito**` só deixar de virar "asterisco asterisco" no plano pago, o plano gratuito soa quebrado. Por isso a OS-040 (sanitização de símbolos) e a OS-039 (navegação por trecho) são do nível simples | Decisão do dono do projeto, com a intenção declarada de transformar o MVP em produto vendável. **Ressalva técnica levantada e aceita na mesma conversa:** "humanização da fala" tem teto no modelo de voz, não no texto — o Kokoro-82M é pequeno e não tem controle de emoção, então uma LLM ajustando pontuação melhora respiro/ritmo mas **não** faz o Kokoro atuar. O salto real de humanização vem de trocar o `Speaker`, que a arquitetura de plugins já prevê desde a decisão #1 — daí o TTS cloud ser o coração do premium, e não um complemento |

## 4. Componentes e status individual

| Componente | Status | Última OS | Observações |
|---|---|---|---|
| `core/models.py` | concluído (testado) | OS-042 | 5 modelos Pydantic implementados com validações de status; `Book.error_message: str \| None` adicionado na OS-018, `Book.chunk_total: int \| None` na OS-024 (None até a síntese começar), `Book.language: str \| None` na OS-025 (None = detecção automática), `Book.status` ganha `"paused"` na OS-032, e **OS-042**: `Book.status` ganha `"pending_confirmation"` + `Book.estimated_cost: float \| None`, `Book.cost_confirmed: bool`, `Book.cost_degraded: bool` (trava de custo); `Job.priority: int = 0` adicionado na OS-032 (maior número = atendido primeiro) |
| `core/pipeline.py` | concluído (testado) | OS-043 | `extract_with_fallback()`, `extract_clean_text()`, `count_text_chunks(text, max_chars=None)` (OS-022, conta chunks sem sintetizar), `estimate_cost(text)` (OS-042: caracteres sanitizados × `cost_per_char` do Speaker configurado, lido antes de qualquer síntese) e `synthesize_text(text, chapter_id, max_chars=None, on_chunk=None, skip_sequences=None, lang_code=None)` — `on_chunk` (OS-021) é chamado com cada chunk logo após a síntese; `skip_sequences` (OS-022) pula as sequences já sintetizadas numa tentativa anterior; `lang_code` (OS-025) força o idioma do engine em todos os chunks (None = detecção automática); `speaker_name` (OS-042) sobrescreve o Speaker desta chamada (None = configurado; usado na degradação por teto). Todos opcionais: sem eles o comportamento é exatamente o da OS-009 . **OS-043:** `synthesize_text` passa a respeitar `speaker.max_request_chars` (divide o texto do chunk em pedaços que não cortam palavra e concatena os áudios num único `AudioChunk`) e a retentar `TransientSpeakerError` com backoff exponencial (`retry.max_attempts`/`base_delay_seconds`/`max_delay_seconds` da config); `PermanentSpeakerError` e demais exceções sobem na hora. **OS-027:** `detect_chapters(pdf_path, total_pages=None)` (nível 1 do TOC via `fitz.get_toc()`, fallback sintético de 10 páginas), `extract_chapters(pdf_path)` (limpeza por capítulo) e `sequence_offset` em `synthesize_text()` para manter a `sequence` global entre capítulos . **OS-036:** o nível do TOC passa a ser escolhido por cobertura (o mais raso cujo maior capítulo caiba em `MAX_CHAPTER_FRACTION`=25%), com preferência por título descritivo quando número e nome estão em níveis diferentes na mesma página, subdivisão de capítulo desproporcional e cobertura das páginas órfãs iniciais |
| `core/config.py` | concluído (testado) | OS-043 | `load_config()` lê `config.yaml` e retorna `Config(extractor, speaker, queue)`; **OS-042: `Config.max_cost_per_book: float \| None` (teto por livro) e `Config.fallback_speaker: str = "kokoro"` (voz local da degradação); OS-043: `Config.retry_max_attempts`/`retry_base_delay_seconds`/`retry_max_delay_seconds` (backoff de falha transitória, lidos do bloco `retry` do yaml)** |
| `plugins/registry.py` | concluído (testado) | OS-011 | `EXTRACTORS = {"pymupdf", "tesseract"}`, `SPEAKERS = {"kokoro"}`, `QUEUES = {"sqlite": SQLiteJobQueue}`, conforme `ARQUITETURA.md` seção 4.4 |
| `plugins/extractors/base.py` | concluído (testado) | OS-003 | Classe abstrata `Extractor` com `supports()` e `extract()` |
| `plugins/extractors/pymupdf_extractor.py` | concluído (testado) | OS-049 | **OS-049:** `extract()` usa `get_text("dict")` e separa blocos do PDF com linha em branco (`BLOCK_SEPARATOR`), preservando `\n` simples DENTRO do bloco — regra que é contrato: sem ela a `_fix_hyphenation` da OS-035 não recola "demons-\ntração". Contrato `Extractor` inalterado. Não classifica estilo ainda (citação, itálico, nota de rodapé a 8,0pt e cabeçalho corrente a 9,3pt seguem narrados como prosa) — OS própria. `PyMuPDFExtractor` com suporte a PDF nativo e image-only |
| `plugins/extractors/tesseract_ocr.py` | concluído (testado) | OS-006 | `TesseractOCR` com fórmula de confidence aprovada (decisão #9) |
| `plugins/extractors/easyocr_extractor.py` | concluído (testado) | OS-017 | `EasyOCRExtractor` — terceiro elo da cadeia de OCR (decisão #13). `_get_reader()` lazy (mesmo padrão de `KokoroSpeaker._get_pipeline`), mockado por completo nos testes automatizados. Validação empírica real (fora dos testes) encontrou confiança `0.7644` para texto legível — abaixo do threshold `0.85` reaproveitado da decisão #9/#13; ver seção 6 e `docs/report/OS-017-report.md` |
| `plugins/speakers/base.py` | concluído (testado) | OS-043 | Classe abstrata `Speaker` com `synthesize()` e `cost_per_char`; **OS-043: contrato ganha `max_request_chars` (limite de caracteres por requisição; `None` = sem limite declarado, comportamento atual) e as exceções `SpeakerError`/`TransientSpeakerError`/`PermanentSpeakerError` para o pipeline classificar o retry**; `synthesize(text, voice=None, lang_code=None)` — `lang_code` adicionado na OS-025 (extensão aditiva, mesma regra das OS-021/022) |
| `plugins/speakers/kokoro_speaker.py` | concluído (testado) | OS-045 | **OS-045 (ritmo):** `NARRATION_SPEED = 0.90` substitui o `speed=1.0` fixo (178 WPM medidos → 140,5). O texto é segmentado na pontuação (`_split_into_pause_segments`), cada segmento é sintetizado à parte, tem as bordas aparadas (`_trim_silence`, ~208 ms de padding do modelo em cada ponta) e recebe silêncio calibrado por `PAUSE_MS_BY_MARK` — vírgula 250, ponto-e-vírgula/dois-pontos 450, ponto 650, interrogação 650, exclamação 750 (acima do ponto de propósito: paliativo por tempo, já que o modelo não tem ênfase) e parágrafo 1100. Aparar ANTES de inserir é o que dá controle exato: sem isso todo vão ficaria fixo em ~400 ms. `_juntar_falsas_fronteiras` usa `is_false_sentence_boundary` de `processing/chunker.py` para 'Dr. Silva' não ganhar pausa de fim de frase — **primeira vez que um plugin importa `processing`, pendente de ratificação (ver relatório da OS-045, seção 6)**. A velocidade foi calibrada COM as pausas aplicadas: medido só na articulação, 0.80 dava o alvo, mas o silêncio conta no tempo total e derrubava para ~125 WPM. **Histórico até a OS-043:** `KokoroSpeaker.synthesize()` chama `pipeline(text, voice=voice, speed=speed)` (G2P real, OS-019). Detecta o idioma via `langdetect` e escolhe `lang_code`/voz do Kokoro, com cache de um pipeline por idioma (OS-020); idioma sem mapeamento, indisponível (`j`/`z`) ou texto curto degradam pro inglês, nunca erro (OS-025: `lang_code` recebido força o idioma e pula a detecção). **Desde a OS-034, para idiomas via espeak (todos exceto `a`/`b`), mede o texto com `pipeline.g2p()` e, se passar de 510 fonemas, divide por fronteira natural (`;`/`:`/`,` preferidos, depois espaço entre palavras — nunca corta palavra ao meio) antes de sintetizar, concatenando num único `AudioChunk`** — o Kokoro truncava silenciosamente (`ps[:510]`) nesses idiomas; inglês continua no caminho `en_tokenize` do próprio engine, inalterado. `_build_pipeline()` é o único ponto que toca o engine — sempre mockado nos testes . **OS-037:** `_apply_phonetic_map()` troca termos do `phonetic_map.yaml` antes da divisão por orçamento de fonemas (fronteira de palavra, case-insensitive); mapa ausente/malformado degrada para 'sem substituição' . **OS-043 (regressão):** o Kokoro **não** declara `max_request_chars` (continua `None`), então a divisão continua sendo interna por fonemas — o pipeline envia o texto inteiro numa chamada, comportamento de áudio idêntico |
| `processing/cleaner.py` | concluído (testado) | OS-035 | `clean_text(pages)` remove linhas repetidas em ≥2 páginas (header/footer) e corrige hifenização de quebra de linha; preserva parágrafos . **OS-035:** `_join_wrapped_lines()` une linhas que continuam a mesma frase (roda após a hifenização), preservando linha em branco como fronteira de parágrafo — cada `\n` virava uma pausa audível, porque o Kokoro divide em `split_pattern=r'\n+'` |
| `processing/chunker.py` | concluído (testado) | OS-035 | `chunk_text(text, max_chars=1000)` divide por sentença via `re`, nunca corta sentença ao meio. `DEFAULT_MAX_CHARS` revertido de `480` (OS-018, calibrado em cima do limite errado — decisão #14) para `1000` (razão original da OS-008: nº de chamadas ao Speaker por capítulo, tamanho previsível de `AudioChunk`) — o comentário que dizia "o Kokoro lida com o limite de fonemas internamente" foi corrigido na OS-034: isso só vale para inglês; o tratamento de frase que estoura 510 fonemas nos demais idiomas acontece no `KokoroSpeaker`, não aqui . **OS-035:** ponto de abreviação (`Dr.`, `pág.`, `etc.`) e inicial de nome deixam de criar falsa fronteira de sentença (lista local, sem spaCy/NLTK) |
| `processing/sanitizer.py` | concluído (testado) | OS-044 | **OS-044:** `sanitize_text` também expande a notação que o G2P do espeak pronuncia errado em pt, tudo medido no G2P real antes de entrar: abreviações via `ABBREVIATION_TO_WORD` (`séc.`→"século", `cap.`→"capítulo", `Sr.`→"senhor", `pp.`→"páginas", `cf.`→"conforme"... — o ponto é consumido junto, o que também evita fronteira de sentença falsa no chunker), horas (`15h30`→"15 e 30", `15h`→"15 horas"), datas brasileiras com mês validado (`12/03/2019`→"12 de março de 2019"; `20/25/2019` e `custo/benefício` intactos) e intervalos (`páginas 10-15`→"10 a 15", só com substantivo de `RANGE_ANCHORS` antes — sem essa âncora não dá para distinguir de subtração legítima; o travessão curto `–` dispensa a âncora). Ordem interna é contrato: abreviações **antes** de intervalos, senão `págs. 10-15` não é reconhecível. **Não expande o que já está certo:** `Dr.`, `Dra.`, `etc.`, `1º`, `½`, `¾`, `™`, `®`, `@`, `+`, `=` foram medidos e têm teste de não-regressão. Sinais de **expressividade** (`...`, `…`, `—`, aspas curvas, parênteses, `?!`, CAIXA ALTA) também foram medidos e já funcionam — nada a fazer. Limitação: `RANGE_ANCHORS` é lista fechada (intervalo com substantivo fora dela segue lido como subtração — lado seguro do trade-off) e ordinais sem o caractere correto (`1o` em vez de `1º`) seguem lidos como "um o" (fora do escopo aprovado, no backlog). **OS-040:** remove, de forma local e determinística, o que não se narra, antes do chunking: marcadores de markup (`**negrito**`/`*itálico*`/`` `código` ``/`# título`/`> citação`/itens de lista `- * + N.` — conteúdo preservado), símbolos mapeados para português (`≠`→"diferente de", `±`, `≈`, `→`, `%`, `°`, `§`, moedas...), linhas de separador de tabela removidas e linhas de dados viram células unidas por vírgula, URLs→"link" e e-mails→"endereço de e-mail" (pontuação de fim de frase preservada) e blocos de código cercados (```/~~~) viram "trecho de código omitido". Aplicado em `pipeline.synthesize_text()` e `pipeline.count_text_chunks()` (o total bate com o que a síntese produz). **Anti-falso-positivo por design:** prosa comum, travessão de diálogo (`—`, U+2014), asterisco/hífen soltos e citação indentada passam intactos — a detecção de bloco de código é só a cercada, deliberadamente, para não comer prosa indentada (decisão documentada na OS-040). Limitação registrada: código inline de uma linha (ex: `def x():`) não é detectado para não arriscar falso positivo |
| `api/` (FastAPI) | concluído (testado) | OS-042 | `api/main.py` (app + lifespan que roda `db.init_db()` e `audio_store.init_db()`), `api/routes_books.py` (`POST /books`, que desde a OS-025 aceita o campo de formulário opcional `language`; `GET /books` — listagem, OS-015 — `GET /books/{id}/status`, com `error_message` quando `status == "error"` desde a OS-018, `chunks_done`/`chunks_total` desde a OS-024, `title` desde a OS-033 e **`estimated_cost`/`cost_confirmed`/`cost_degraded` desde a OS-042** — **`POST /books/{id}/confirm` desde a OS-042 (confirma a estimativa de um livro em `pending_confirmation`: marca `cost_confirmed`, volta o status a `uploaded` e re-enfileira; 404 se não existe; 409 se não está aguardando)** — `POST /books/{id}/prioritize` desde a OS-032 (200; 404 se o livro ou o `Job` dele não existir; 409 se `ready`/`error`, decisão #21) e `DELETE /books/{id}` desde a OS-023, com 404/409 — desde a OS-033, `_BLOCKED_DELETE_STATUSES` é só `{"extracting", "processing", "synthesizing"}` (decisão #22): `uploaded` e `paused` são deletáveis) e `api/routes_audio.py` (`GET /books/{id}/audio`, `GET /books/{id}/audio/{sequence}`) |
| `plugins/queues/base.py` | concluído (testado) | OS-032 | `JobQueue` (ABC) — `enqueue`, `claim_next`, `mark_done`, `mark_failed`, `get_job` (OS-011) + `requeue_orphaned()` (OS-022, decisão #15) + `delete_jobs_for_book()` (OS-023, decisão #16) + `prioritize()`/`should_yield()`/`requeue()`/`get_job_for_book()` (OS-032, decisão #21), tudo espelhado em `ARQUITETURA.md` seção 4.3 |
| `plugins/queues/sqlite_queue.py` | concluído (testado) | OS-032 | `SQLiteJobQueue` — tabela `jobs` no mesmo arquivo de `storage/db.py` (`books.db`), com coluna `priority INTEGER NOT NULL DEFAULT 0` desde a OS-032; `claim_next()` atômico via `BEGIN IMMEDIATE` + `UPDATE ... WHERE status='queued'`, ordenando por `priority DESC, rowid` (FIFO quando tudo `0`); `requeue_orphaned()` (OS-022) reseta `running` → `queued` na mesma transação `BEGIN IMMEDIATE`; `delete_jobs_for_book()` (OS-023) remove os `Job`s de um `book_id`; `prioritize()` (OS-032) dá a um `Job` prioridade `max+1` de todos os Jobs (maior que qualquer pendente, queued ou running); `should_yield()` (OS-032) devolve `True` se existe `Job` `queued` com prioridade maior; `requeue()` (OS-032) devolve um `Job` individual a `queued` preservando prioridade; `get_job_for_book()` (OS-032) acha o `Job` de um `book_id` |
| `worker/tasks.py` | concluído (testado) | OS-043 | `process_job(job)` persiste cada `AudioChunk` incrementalmente via `on_chunk` (OS-021), marca `Book.status = "synthesizing"` antes da síntese e, desde a OS-022, pula as `sequence`s já persistidas + checa consistência (`_resume_inconsistency()`, decisão #16); desde a OS-024 grava `chunk_total` via `db.set_book_chunk_total()` antes de marcar `synthesizing`; desde a OS-025 repassa `book.language` pra `synthesize_text()`; desde a OS-032, dentro do `on_chunk`, consulta `queue.should_yield(job.id)` e, se `True`, interrompe a síntese de forma cooperativa (exceção sentinela `_YieldRequested`) — o `Job` volta a `queued` via `queue.requeue()` preservando prioridade (não é falha) e o `Book` vira `paused`, sem apagar nenhum `AudioChunk`; desde a OS-042, antes da síntese, soma `pipeline.estimate_cost()` dos capítulos, persiste via `db.set_book_estimated_cost()` e aplica a trava de custo: estimativa > 0 sem `cost_confirmed` deixa o livro em `pending_confirmation` (Job encerrado; a confirmação re-enfileira); estimativa acima de `max_cost_per_book` marca `cost_degraded` e sintetiza com `fallback_speaker` (nunca o pago); **desde a OS-043, retry de falha transitória esgotado (`TransientSpeakerError`) vira `Book` "error" com mensagem explícita de que o áudio já persistido está preservado (retomável via OS-022)**; `run_worker(poll_interval, max_iterations)` chama `requeue_orphaned()` uma vez antes do loop de polling; `python -m worker.tasks` para rodar manualmente . **OS-027:** sintetiza capítulo a capítulo (`extract_chapters()`), persiste os capítulos e atribui o `chapter_id` real a cada `AudioChunk`, com `sequence` global e contínua |
| `storage/` | concluído (testado) | OS-028 | `db.py` (OS-010, `list_books()` na OS-015, `error_message` e `update_book_status()` na OS-018, `delete_book()` na OS-023, `chunk_total` + `set_book_chunk_total()` na OS-024, coluna `language` na OS-025), `uploads.py` (OS-012, `delete_pdf()` na OS-023) e `audio_store.py` (OS-013 — `persist_chunks`/`list_chunks`/`get_chunk`/`delete_chunks` na OS-023, tabela `audio_chunks` no mesmo `books.db`, arquivos em `storage/audio/{book_id}/{sequence}.wav`) concluídos e testados; tabela `jobs` de `plugins/queues/sqlite_queue.py` também no mesmo arquivo . **OS-027:** tabela `chapters` (metadados apenas, sem o texto) + `create_chapters()`/`list_chapters()` em `db.py` . **OS-028:** `progress_store.py` (tabela `reading_progress`, uma linha por livro via UPSERT — só a posição atual, sem histórico) |
| `player/` (frontend) | concluído (testado) | OS-042 | HTML/CSS/JS puro (decisão #12): upload, polling, playback sequencial, play/pause, velocidade, resume via `localStorage` (OS-014) + seção "Meus livros" consumindo `GET /books`, botão "Atualizar lista", auto-atualiza após upload (OS-016) + botão "Deletar" por item com `window.confirm` e limpeza do player/`localStorage` quando o livro aberto é deletado (OS-023) + barra `<progress>` de síntese com fallback pro texto quando `chunks_total` é `null` (OS-024) + **seletor de idioma no formulário de upload (OS-025)** + **playback de áudio parcial (OS-030): `pollBook()` lê status e chunks a cada ciclo, toca com ≥1 chunk sem esperar `ready`, anexa os novos reancorando a posição pela `sequence`, estado "Aguardando próximo trecho..." no fim da fila com retomada automática, e livro em `error` continua tocável com o que já existe** + **"Processar agora" (OS-032): botão por item da lista (ao lado do "Deletar", com `event.stopPropagation()`), habilitado só para `uploaded`/`paused`, que chama `POST /books/{id}/prioritize` e atualiza a lista; livro `paused` para o polling (`pollBook` para em `ready`/`error`/`paused`), esconde a barra de progresso e mostra "Pausado — tocando o que já foi sintetizado." com o áudio já gerado tocável** + **UX da lista (OS-033): cabeçalho do player mostra o título do livro em vez do UUID (`playerTitle` usa o título do clique na lista, do `title` de `fetchStatus()` ou do `title` salvo em `localStorage` via `saveState`/`loadSavedState`); clicar num item da lista preenche o campo "Abrir livro existente"; `deleteBook()` mostra o `detail` real da API quando o delete falha; `statusLabel()` traduz `uploaded` → "Na fila — aguardando processamento" (e demais status) na lista e no `#player-status` — só exibição, `Book.status` não muda**. Servido por `api/main.py` (`StaticFiles` em `/`). `test_player_static_files_are_served` e `test_player_upload_form_has_language_select_with_auto_default` passam; verificação manual em navegador real da OS-033 feita com Chrome real (`docs/report/OS-033-report.md` seção 4); a do seletor de idioma da OS-025 e a do "Processar agora" da OS-032 seguem pendentes (ver seção 6) . **OS-029:** seletor de capítulos clicável (`GET /books/{id}/chapters`) com marcação do capítulo em reprodução e capítulos ainda não sintetizados desabilitados, indicador de posição ("Capítulo 2 de 3 — ... · trecho 4 de 6") e banner de retomar alimentado pelo progresso do servidor (OS-028). **OS-039:** navegação por trecho — botões "◀ Anterior" e "Próximo ▶" na seção `#controls` + atalhos de teclado (← anterior, → próximo, espaço play/pause, ignorados quando o foco está em input/select/textarea); "Anterior" segue o padrão de podcast (>3s no trecho reinicia o atual, senão volta um trecho) e é desabilitado no primeiro trecho; "Próximo" é desabilitado enquanto o trecho seguinte ainda não foi sintetizado; navegar atualiza capítulo em foco e indicador (OS-029) e grava a posição no servidor na hora (OS-028). Verificação manual em navegador real feita (`docs/report/OS-039-report.md` seção 4) |

Valores possíveis de status: `não iniciado` · `em andamento` · `implementado sem testes` · `concluído (testado)` · `bloqueado`.

## 5. Backlog priorizado (próximas OS candidatas)

1. **OS-001 — Bootstrap do repositório e instalação de dependências** — status: concluída
2. **OS-002 — `core/models.py`** — modelos de dados base — status: concluída
3. **OS-003 — `plugins/extractors/base.py` + `PyMuPDFExtractor`** — status: concluída
4. **OS-004 — `plugins/speakers/base.py` + `KokoroSpeaker`** — status: concluída
5. **OS-005 — Spike: heurística de confiança de OCR** (decisão #5) — status: concluída, heurística aprovada (decisão #9)
6. **OS-006 — `plugins/extractors/tesseract_ocr.py`** — `TesseractOCR` usando a heurística aprovada — status: concluída
7. **OS-007 — `core/pipeline.py`** — orquestração síncrona mínima ligando extractor → speaker (+ preenche `plugins/registry.py` e `core/config.py`) — status: concluída
8. **OS-008 — `processing/cleaner.py` + `processing/chunker.py`** — status: concluída
9. **OS-009 — Ligar cleaner/chunker em `core/pipeline.py`** — substitui a síntese de texto inteiro numa chamada só — status: concluída
10. **OS-010 — API mínima** (`POST /books`, `GET /books/{id}/status`, síncrona, SQLite) — status: concluída
11. **OS-011 — Contrato `JobQueue` + `SQLiteJobQueue`** — status: concluída
12. **OS-012 — Liga `JobQueue` em `worker/tasks.py` e na API** — `POST /books` passa a enfileirar em vez de processar inline — status: concluída
13. **OS-013 — `storage/audio_store.py` + servir áudio pela API** — status: concluída
14. **OS-014 — Player web básico** (play/pause, velocidade, retomar posição — HTML/CSS/JS puro) — status: concluída, verificação manual em navegador feita na revisão (ver `docs/os/OS-014-player-web-basico.md` e `docs/report/OS-014-report.md` seção 6.1)
15. **OS-015 — `GET /books` (listagem)** — status: concluída
16. **OS-016 — Liga a listagem de livros na UI do player** — status: concluída, verificação manual em navegador feita na revisão (ver `docs/os/OS-016-listagem-no-player.md` e `docs/report/OS-016-report.md` seção 6.1)
17. **OS-017 — `plugins/extractors/easyocr_extractor.py`** (terceiro elo da cadeia de OCR, decisão #13) — status: concluída, com achado empírico sobre o threshold `0.85` (ver seção 6 e `docs/report/OS-017-report.md`)
18. **OS-018 — Corrige falha de síntese em texto denso** ("Phoneme string too long") — bug real encontrado em uso — status: concluída (sintoma corrigido; causa raiz veio a seguir na OS-019, mesmo branch/PR)
19. **OS-019 — Corrige a causa raiz: `KokoroSpeaker` usa a API errada do Kokoro** (pula G2P) — status: concluída, ver `docs/report/OS-019-report.md` — PR #16 cobre OS-018 + OS-019 juntas
20. **OS-020 — Detecção automática de idioma pro Kokoro** — bug real encontrado em uso (PDF em português sintetizado em inglês) — status: concluída, com achado sobre japonês/mandarim indisponíveis neste ambiente (ver `docs/report/OS-020-report.md`)
21. **OS-021 — Entrega incremental de áudio** (persistir cada chunk assim que fica pronto, não só no final do livro inteiro) — status: concluída (ver `docs/report/OS-021-report.md`)
22. **OS-022 — Retomar processamento interrompido** (depende da OS-021) — status: concluída (ver `docs/report/OS-022-report.md`)
23. **OS-023 — Deletar livro** (`DELETE /books/{id}` + botão na UI) — achado em teste real: não existe nenhuma forma de remover um livro — status: concluída (ver `docs/report/OS-023-report.md`)
24. **OS-024 — Progress bar real de síntese** (expõe `chunks_done`/`chunks_total` na API, reaproveitando `count_text_chunks()` da OS-022) — achado em teste real: só existe texto cru de status, sem noção de quanto falta — status: concluída (ver `docs/report/OS-024-report.md`)
25. **OS-025 — Seleção manual de idioma** (override opcional no upload, detecção automática da OS-020 continua sendo o padrão) — achado em teste real: sem forma de corrigir a detecção quando ela erra — status: concluída (ver `docs/report/OS-025-report.md`)
26. **OS-026 — Nome amigável em vez de UUID na UI** (`GET /books/{id}/status` ganha `title`) — achado em teste real: cabeçalho do player e `localStorage` só mostram o UUID cru — status: **substituída pela OS-033**, nunca executada; escopo absorvido integralmente (ver `docs/os/OS-026-nome-amigavel-na-ui.md`, marcada no topo)
27. **OS-030 — Tocar áudio parcial enquanto o livro ainda sintetiza** — era o "ligar na UI" que a OS-021 deixou explicitamente pra depois; medido em uso real, 2265 chunks (40h de áudio) prontos no disco e inacessíveis — status: concluída, com verificação em navegador real (ver `docs/report/OS-030-report.md`)
28. **OS-031 — Spike: performance da síntese** (batching na GPU, tamanho de chunk, overhead por chamada) — linha de base medida: 1,34 s/chunk, ~14s de áudio por segundo de processamento na RTX 3060. Spike de medição/recomendação, não de implementação (mesmo formato da OS-005) — status: concluída — recomendação "não mudar nada" proposta como decisão #19 (ver `docs/report/OS-031-report.md`)
29. Reenviar livros já processados antes da OS-019 (pronúncia incorreta) — inclusive o "Security Engineering" que originou a investigação
30. Revisitar o threshold de confiança `0.85` especificamente para `EasyOCRExtractor`, à luz do achado da OS-017 — recomendado antes do item 31
31. Ligar `EasyOCRExtractor` na cadeia de fallback de `core/pipeline.py`
32. **OS-027 — Detecção de capítulos via TOC do PDF** (com fallback sintético quando não há TOC) — 1ª parte do "Visualizador do livro"; `AudioChunk.chapter_id` passa a refletir capítulos reais, não mais um id único por livro — status: **concluída** (ver `docs/report/OS-027-report.md`)
33. **OS-028 — Persistência de progresso de leitura no servidor** (substitui/complementa o `localStorage`) — 2ª parte do "Visualizador do livro", sem dependência técnica dura da OS-027 (pode trocar de ordem se conveniente) — status: **concluída** (ver `docs/report/OS-028-report.md`)
34. **OS-029 — UI do Visualizador** (seletor de capítulos + indicador de posição) — depende das OS-027 e OS-028 — status: **concluída**, verificação manual em navegador real feita (ver `docs/report/OS-029-report.md` seção 4)
35. **OS-032 — Preempção de fila: "Processar agora"** (botão por livro que pausa a síntese em andamento e põe o escolhido na frente) — status: **concluída** (ver `docs/report/OS-032-report.md`)
36. **OS-033 — UX da lista de livros: nome amigável, estado da fila e delete de enfileirado** — consolida a OS-026 (substituída, nunca executada) com os três achados de teste real de 2026-08-05: delete de livro `uploaded`, propagação do motivo real do erro de delete, clique na lista preenchendo o campo de abrir, e rótulo "na fila" em vez de `uploaded` cru — status: **concluída** (ver `docs/report/OS-033-report.md`)
37. **OS-034 — Corrige truncamento silencioso de áudio em idiomas não-ingleses** — status: **concluída** (ver `docs/report/OS-034-report.md`)
38. **OS-035 — Juntar linhas quebradas do PDF** — achado em uso real ("narração picotada") e **medido**: o Kokoro divide o texto em `\n` (`split_pattern`), e o `cleaner.py` não junta linhas que continuam a mesma frase — um parágrafo comum vira **5 segmentos de síntese em vez de 1**, ou seja ~12 pausas artificiais por chunk. Conserto local, sem dependência nova, sem custo. Inclui abreviações (`Dr.`, `pág.`) que hoje viram falsa fronteira de sentença no chunker. status: **concluída** — medido: parágrafo real passou de 5 segmentos de síntese para 1, sem perda de palavras (ver `docs/report/OS-035-report.md`)
39. **OS-036 — Corrige detecção de capítulos** (nível do TOC, capítulo-monstro e páginas órfãs) — três defeitos da OS-027 confirmados com o "Arquitetura Limpa": (a) "só nível 1" devolve só o front matter quando a estrutura real está aninhada (263 entradas no TOC, 4 no nível 1); (b) o último capítulo absorve o resto do livro ("Sobre o Autor" = 415 páginas, 320 chunks); (c) páginas antes do 1º item do TOC são **descartadas silenciosamente** (as 1–17, com texto real). Inclui a perfumaria do título no campo "Abrir livro existente". status: **concluída** — medido no PDF real: 4 → 35 capítulos, maior capítulo de 415 pág (93%) → 76 pág (17%), cobertura a partir da página 1 (ver `docs/report/OS-036-report.md`)
40. **OS-037 — Dicionário de substituição fonética local** — mapa local (sem API) para termos que o G2P do espeak erra sempre. Não conserta o sotaque geral, conserta termo a termo — status: **concluída** — 7 entradas com evidência medida em frase; `cache` testado e rejeitado por piorar a fonemização (ver `docs/report/OS-037-report.md`)
41. **OS-038 — "Ajudante LLM" (nível médio)** — contrato `TextNormalizer` + `NoOpNormalizer` (padrão, sem rede) + `LLMNormalizer` via endpoint compatível com OpenAI, opt-in por livro, guarda-corpo em três camadas contra alteração do texto do autor, cache e custo somado à trava da OS-042. Custo estimado: **~US$ 0,16–0,87 por livro** (duas ordens de grandeza abaixo do TTS cloud do premium) — status: **concluída**; falta o dono validar o provedor com a chave real (`scripts/validate_normalizer.py`) — ver `docs/report/OS-038-report.md`
42. **OS-039 — Navegação por trecho no player** (botões anterior/próximo + atalhos de teclado) — status: **concluída** (ver `docs/report/OS-039-report.md`)
43. **OS-040 — Sanitização de markup, símbolos e blocos não-narráveis** — status: **concluída** (ver `docs/report/OS-040-report.md`)
44. **OS-041 — Spike: comparação de provedores de TTS cloud** (qualidade em pt-BR + preço atual real, mesma amostra em 2–3 provedores e no Kokoro como linha de base) — define o motor do **nível premium** (decisão #23). **Faz chamadas pagas reais, fora da suíte** — exige credenciais e aprovação de orçamento pequeno do dono. Medido para dimensionar: livro técnico típico do acervo tem **500–650 mil caracteres** (Arquitetura Limpa 533k, Programador Pragmático 649k) — status: **executada parcialmente (2026-08-06):** preços oficiais levantados, script `scripts/spike_tts_cloud.py` versionado e baseline Kokoro sintetizado; parte empírica cloud **bloqueada por credenciais** (decisão do dono pela opção 2 — sem chamadas pagas); recomendação de motor suspensa até o dono fornecer credenciais (ver `docs/report/OS-041-report.md`)
45. **OS-042 — Trava de custo** (estimativa com confirmação **+** teto de segurança, as duas conforme decisão do dono) — liga o `Speaker.cost_per_char`, que existe desde a OS-004 e **nunca foi lido por nada**. Livro de custo zero (Kokoro) segue sem fricção nenhuma. **Independe da OS-041** — status: **concluída** (ver `docs/report/OS-042-report.md`)
46. **OS-043 — Preparar o pipeline para `Speaker` remoto** (limite declarado por engine + retry com backoff) — remove duas suposições que só valem para síntese local: o limite medido em fonemas pelo G2P do Kokoro (cloud mede em caracteres) e "qualquer falha vira livro com erro" (com 683 chamadas de rede por livro, 1% de intermitência derruba ~7 vezes). **Independe da OS-041** — status: **concluída** (ver `docs/report/OS-043-report.md`)
47. **OS-044 — Notação que o espeak lê errado** — abreviações (`séc.`→"sék", `Sr.`→"ésse-érre"), horas (`15h30`→"quinze agá trinta"), datas (`12/03/2019`→"doze zero três...") e intervalos (`10-15`→"dez menos quinze") corrigidos no `sanitize_text`, ou seja, no **nível simples** — coerente com a decisão #23: melhoria determinística e sem custo não pertence ao plano pago. O ajudante LLM da OS-038 já cobria esses casos, mas só para quem paga e marca o opt-in. Escopo definido por medição no G2P real: `Dr.`, `Dra.`, `etc.`, `1º`, `½`, `™`, `@` já eram pronunciados certo e ficaram intocados — status: **concluída** (ver `docs/report/OS-044-report.md`)

48. **OS-045 — Ritmo e temporização da narração** — a narração saía a 178 WPM com pausas de 169/195/203 ms para vírgula/ponto-e-vírgula/ponto, contra 95 ms sem pontuação nenhuma: **não havia hierarquia rítmica**, e era isso (não a velocidade isolada) que cansava a escuta. Agora 140,5 WPM e pausas de 285/485/690/1135 ms, todas dentro do *Guia Prático de Ritmo e Temporização*. A pausa de parágrafo passou a existir — antes o `chunk_text` juntava tudo com espaço e ela morria antes do Speaker — status: **concluída** (ver `docs/report/OS-045-report.md`)
48B. **OS-049 — Estrutura do documento na extração** — o `PyMuPDFExtractor` usava `get_text()` simples, que achata o PDF em linhas sem hierarquia: título de seção chegava grudado na prosa e a pausa de parágrafo da OS-045 **nunca disparava em PDF real**. Agora usa `get_text("dict")` e separa blocos com linha em branco, **sem alterar o contrato `Extractor`** — a estrutura cabe no texto simples porque a OS-045 já converte linha em branco em pausa. Verificado no Prefácio do "Programador Pragmático": **0 → 68** fronteiras de parágrafo, e o título ganhou pausa antes e depois. Linhas do mesmo bloco seguem com `\n` simples, por contrato: a `_fix_hyphenation` da OS-035 depende disso para recolar palavra partida — status: **concluída** (ver `docs/report/OS-049-report.md`)
### Decididas na sessão de 06/08/2026, ainda não abertas como OS

49. **Escolha de voz pelo usuário (candidata a OS-046)** — hoje a voz é fixa por idioma (`VOICE_BY_LANG_CODE`, `pf_dora` em pt). O dono validou as três vozes disponíveis em português por escuta e decidiu expor **`pf_dora`, `pm_alex` e `pm_santa` como opção de produto**, sem misturas. Decisão de escopo tomada junto: blend de vozes **funciona** (o `KPipeline` aceita um tensor combinado no lugar do nome, confirmado por teste) e três proporções foram avaliadas, mas foram **descartadas** — voz de catálogo é mais previsível e explicável para o usuário final. O catálogo em português tem só essas três vozes; não existe voz "noticiosa" ou "artística" rotulada. Provável forma: campo por livro, no mesmo molde do seletor de idioma da OS-025 (coluna em `books`, campo no `POST /books`, `<select>` no player).

50. **Prompt de preparação prosódica, separado do normalizador (candidata a OS-047)** — o `SYSTEM_PROMPT` do `LLMNormalizer` (OS-038) trata de *notação* (números por extenso, abreviações) e trata pontuação só de passagem. A ideia é um prompt **explícito e separado** para preparação prosódica: encurtar frase longa, inserir vírgula como ponto de respiração, dar corrida ao modelo antes de uma interrogação. **Com guarda-corpo calibrado à parte** — o da OS-038 mede divergência de tamanho, que é a métrica errada aqui: preparação prosódica muda pontuação sem mudar o texto, e o guarda-corpo atual ou deixaria passar demais ou barraria de menos. Tensão a resolver na OS: quanto mais a LLM reescreve, mais perto chega de alterar o texto do autor, que é justamente o que o guarda-corpo existe para impedir. **Teto conhecido:** a LLM reestrutura pontuação, mas não cria entonação que o modelo não tem — o Kokoro-82M não tem controle de emoção nem de ênfase (decisão #23). O salto real segue sendo trocar o `Speaker` (OS-041).

## 6. Riscos e bloqueios conhecidos

- **Pendente (OS-041, decisão do dono de 2026-08-06): credenciais de TTS cloud para fechar o spike do nível premium.** O spike rodou sem chamadas pagas (opção 2 escolhida pelo dono): preços oficiais levantados, script pronto e baseline Kokoro sintetizado, mas a qualidade de voz em pt-BR — o critério decisivo do premium — **não foi ouvida** porque nenhuma credencial existe no ambiente. Para concluir: exportar as env vars do `scripts/spike_tts_cloud.py` (5 listadas no cabeçalho) e aprovar o orçamento pequeno (~12 chamadas de teste, centavos por provedor). Número para dimensionar (levantado 2026-08-06): livro técnico típico custa **US$ 8–16** nos patamares baratos (OpenAI tts-1 US$15/1M chars; Google Neural2 e Polly neural US$16/1M) e **US$ 16–20** no Chirp 3 HD / Polly generative / tts-1-hd (US$30/1M); **ElevenLabs é ~10× mais cara** (créditos, ~US$165/1M no plano Pro) — provavelmente fora do páreo antes da audição. OpenAI `gpt-4o-mini-tts` é o único com controle de emoção/ritmo via instrução (relevante à decisão #23) mas cobra por token, não por caractere — precisa de medição tokens/caractere para fechar custo.
- **Info para a OS-043 (limites por requisição, levantados 2026-08-06):** Polly `SynthesizeSpeech` 3.000 chars cobráveis/chamada (6.000 totais; 100.000 via `StartSpeechSynthesisTask`), Google `text:synthesize` 5.000, OpenAI 4.096, ElevenLabs 5.000. Todos acomodam o `max_chars=1000` atual com folga — nenhum exige mudança no chunker.
- **Em aberto (achado da OS-020): japonês e mandarim estão mapeados mas não funcionam neste ambiente.** `KokoroSpeaker` mapeia `ja`→`j` e `zh`→`z` conforme os `ALIASES` do Kokoro, mas construir esses pipelines falha com `ModuleNotFoundError: No module named 'pyopenjtalk'` (japonês) e `'ordered_set'` (mandarim) — o Kokoro exige `misaki[ja]`/`misaki[zh]`, que não estão instalados (só `misaki[en]` e o `espeak-ng`, que cobre es/fr/hi/it/pt). O `KokoroSpeaker` degrada pro inglês nesse caso em vez de derrubar o livro inteiro (confirmado empiricamente: texto em japonês → detecta `j` → cai pro `a`), então **não é um bug ativo** — mas um PDF em japonês/mandarim vai gerar áudio com pronúncia errada, silenciosamente, do mesmo jeito que o português gerava antes desta OS. Resolver é só instalar as dependências extras, se algum dia importar; não foi feito aqui por estar fora do escopo da OS-020 (que é sobre detecção, não sobre ampliar idiomas suportados).
- **Dívida técnica (achada em uso real, 2026-08-04): não existe migração de schema no SQLite.** `storage/db.py`/`plugins/queues/sqlite_queue.py`/`storage/audio_store.py` só fazem `CREATE TABLE IF NOT EXISTS` — isso cria a tabela se ela não existe, mas nunca adiciona coluna nova numa tabela que já existe. Quando a OS-018 adicionou a coluna `error_message` em `books`, qualquer `books.db` local criado **antes** dessa OS passou a quebrar com `sqlite3.OperationalError: table books has no column named error_message` na primeira tentativa de `POST /books` depois de atualizar o código. Correção imediata: apagar `books.db` (dado de runtime, gitignored, RUNBOOK.md seção 6 já documenta isso) e deixar o schema recriar do zero. **Isso vai se repetir a cada mudança de coluna futura**, com o mesmo erro pouco claro, até existir alguma forma de migração (mesmo que simples, tipo `ALTER TABLE ... ADD COLUMN` condicional a checar se a coluna já existe). Candidato a OS própria se incomodar de novo. Se confirmou de novo na OS-032: a tabela `jobs` ganhou `priority INTEGER NOT NULL DEFAULT 0` e o erro correspondente é `table jobs has no column named priority` (RUNBOOK.md seção 8). **Se repetiu na OS-042:** a tabela `books` ganhou `estimated_cost`, `cost_confirmed` e `cost_degraded` (trava de custo) — apagar `books.db` (ou as três colunas) antes de rodar com o código novo.
- **Limitação conhecida e aceita (OS-022, decisão #15): a retomada de `Job`s órfãos assume um único worker rodando por vez.** Não existe heartbeat/lease — na inicialização, o worker devolve para a fila *todo* `Job` em `running`, sem conseguir distinguir "worker anterior morreu no meio" de "outro worker está processando agora". Com dois workers simultâneos, o segundo a subir faria o primeiro reprocessar/duplicar trabalho. Enquanto for um worker só (decisão #11, `RUNBOOK.md` seção 4), não é problema; se algum dia rodar mais de um, isso precisa virar OS própria antes.
- **Resolvido (OS-021 + OS-022): perda de trabalho ao interromper um livro grande.** Antes da OS-021 nenhum `AudioChunk` era persistido até o livro inteiro terminar; antes da OS-022 um `Job` interrompido ficava órfão em `running` pra sempre (`claim_next()` só enxerga `queued`) e a única saída era reenviar o PDF do zero. Hoje o worker retoma sozinho ao subir, a partir do primeiro chunk não persistido.
- **Resolvido (OS-018 + OS-019, mesmo branch/PR #16):** OS-018 corrigiu o crash `Phoneme string too long: 863 > 510` (enviar "Security Engineering" resultava em `Book.status == "error"`), mas usando a API errada do Kokoro — `pipeline.generate_from_tokens()` com texto bruto pulava o G2P por completo (decisão #14). OS-019 corrigiu a causa raiz: troca pra `pipeline()` (G2P de verdade + chunking automático do próprio Kokoro, confirmado empiricamente com o texto "The quick brown fox..." virando `'ðə kwˈɪk bɹˈWn fˈɑks...'`) e removeu a lógica de divisão manual da OS-018. `DEFAULT_MAX_CHARS` voltou a `1000`. `error_message` exposto em `GET /books/{id}/status` (adicionado na OS-018) continua válido, não mudou. **Todo áudio gerado entre a OS-004 e a OS-019 tem pronúncia incorreta** — `RUNBOOK.md` avisa que esses livros precisam ser reenviados, não há reprocessamento automático. Detalhes em `docs/report/OS-018-report.md` (achado original) e `docs/report/OS-019-report.md` (correção + evidência empírica).
- **Resolvido (OS-030): a UI não segura mais áudio já sintetizado.** O player toca a partir do primeiro chunk persistido, sem esperar `status === "ready"` — verificado em Chrome real contra worker/Kokoro reais (`docs/report/OS-030-report.md` seção 4). A verificação manual da OS-030 exercitou de passagem a barra de progresso da OS-024 (visível durante a síntese, escondida no `ready`), que estava pendente; a da OS-023 (botão "Deletar") continua pendente.
- **Ferramenta de verificação de UI usada na OS-030, não adotada como dependência:** Playwright dirigindo o Chrome do sistema, instalado num venv de scratchpad, com a API e o worker rodando num `cwd` isolado (o `books.db` real não é tocado, já que todos os caminhos de runtime são relativos ao `cwd`). Receita registrada em `docs/report/OS-030-report.md` seção 4 — reutilizável em qualquer OS de UI futura. Adotar (ou não) uma suíte de testes de browser no projeto continua sendo decisão em aberto do dono do projeto, não de agente de execução.
- **Pendente (OS-025): verificação manual em navegador real do seletor de idioma.** O `<select>` do upload, o envio do campo `language` e a tradução `book.language` → lang_code estão cobertos por testes automatizados, mas a experiência fim a fim (escolher idioma, enviar PDF de verdade, ouvir o áudio no idioma correto) não foi exercitada com worker/Kokoro reais nesta entrega. Mesma receita Playwright da OS-030 se aplica quando o dono quiser validar.
- **Pendente (OS-032): verificação manual em navegador real do botão "Processar agora".** A preempção cooperativa (`should_yield` no `on_chunk`), o `requeue` preservando prioridade, o livro `paused` e o endpoint `POST /books/{id}/prioritize` estão cobertos por testes automatizados, mas a experiência fim a fim (dois livros enfileirados, clicar "Processar agora" num deles, ver o outro pausar e o escolhido começar, retomar o pausado de onde parou) não foi exercitada com worker/Kokoro reais — envolve esperar chunks de síntese reais (~1s cada) e o polling do player. Mesma receita Playwright da OS-030 se aplica quando o dono quiser validar. De passagem, exige lembrar do aviso de schema: o `books.db` local precisa ser recriado/alterado para ganhar a coluna `priority` na tabela `jobs`.
- **Resolvido (OS-033): verificação manual em navegador real da UX da lista.** Feita com Chrome real dirigido por Playwright (venv de scratchpad, API em `cwd` isolado de scratchpad — `books.db` real não tocado): abrir livro pela lista mostra o título no cabeçalho e preenche o campo "Abrir livro existente"; o rótulo de fila aparece ("Na fila — aguardando processamento" / "Sintetizando"); sessão restaurada do `localStorage` mostra o título antes do poll (verificado de forma determinística abortando a rota de status); deletar livro enfileirado funciona; deletar livro em síntese mostra o `detail` real ("Book is still processing"). Detalhes e saída no relatório da OS-033 (seção 4).
- **Resolvido:** a verificação manual em navegador exigida pelos DoDs da OS-014 e da OS-016 foi concluída em ambos os casos na revisão pós-entrega, com acesso a navegador real. Único item não exercitado fim a fim nas duas: o clique no seletor nativo de arquivo do SO (barreira de segurança do próprio browser, não do player) — o endpoint que ele chama foi validado via `curl`/`DataTransfer`+`requestSubmit()` e o código revisado. Detalhes em `docs/report/OS-014-report.md` seção 6.1 e `docs/report/OS-016-report.md` seção 6.1.
- Nova ferramenta de dev: `.claude/launch.json` configurado para subir a API (`uvicorn api.main:app`) via preview do navegador em sessões futuras. Lembrar de também rodar `python -m worker.tasks` numa janela separada para o processamento acontecer de verdade.
- **Confirmado em teste real (2026-08-05, sessão pós-merge OS-021/022):** o aviso acima se concretizou — o dono do projeto rodou só o `uvicorn`, sem o worker, e viu 2 livros presos em `status: queued` (nunca reivindicados) e o "Security Engineering" com `Job.status: running` mas sem avançar (1240 chunks já persistidos, órfão — exatamente o cenário que a OS-022 existe para resolver; resolve sozinho assim que um worker sobe, via `requeue_orphaned()`). Também confirmado: o `ROTEIRO_OPERACIONAL_PDV.pdf` (único livro `ready`) teve seu `Job` concluído em `01:11:43`, **antes** do merge da OS-020 (`01:51:05`) — seu áudio não tem detecção de idioma, precisa ser reenviado para testar a feature de verdade (mesmo padrão do item 27 do backlog, seção 5). Nenhum bug de código encontrado nesta investigação — o sintoma percebido ("só um livro abre", "livro grande trava") era 100% explicado pelo worker não estar rodando.
- **Linha de base de performance da síntese, medida em uso real (2026-08-05, "Security Engineering", 3334 chunks):** a síntese **não é** o gargalo percebido. GPU em uso (RTX 3060, `torch.cuda.is_available() == True`; o Kokoro seleciona CUDA sozinho quando disponível), **1,34 s/chunk** nos últimos 100 chunks (a média histórica de 4,57 s/chunk inclui as interrupções/retomadas), **~14 s de áudio gerado por segundo de processamento**, ~74 min para as 40,5h de áudio do livro inteiro. O sintoma percebido ("10 minutos carregando") era a **UI segurando áudio já pronto**: 2265 chunks persistidos e tocáveis enquanto o player esperava `status === "ready"` — **resolvido pela OS-030**. A OS-031 (spike) mediu o custo puro do Kokoro em corpus fixo (0,896 s/chunk, ~63× áudio/tempo) e **recomendou não mudar nada** (decisão #19, pendente de aprovação). **Cautela registrada:** com um worker só e a GPU já saturada durante a síntese (~89% média, medido na OS-031), paralelismo multi-worker tende a competir pelo mesmo recurso escasso em vez de somar throughput — não assumir que "mais fila" = "mais rápido" sem medir.
- Decisão #3 (fila de jobs) resolvida na decisão #11: `SQLiteJobQueue` como plugin, contrato pronto para trocar para Redis depois sem reescrever pipeline/API/worker.
- Achado na revisão pré-OS-013 (`worker/tasks.py` descartava o retorno de `synthesize_text()`, nenhum `AudioChunk` persistido) — corrigido pela OS-013: `worker/tasks.py` agora chama `storage.audio_store.persist_chunks()` antes de marcar o `Book` como `ready`.
- Decisão #13: o terceiro extractor da cadeia de OCR passa a ser `EasyOCRExtractor` (reaproveita o `torch` já instalado via Kokoro), não `PaddleOCR` (~195MB de `paddlepaddle`, framework novo). A fórmula de confidence reaproveita o threshold `0.85` por analogia ao Tesseract.
- **Em aberto (achado da OS-017):** validação empírica real do `EasyOCRExtractor` (2 fixtures, `docs/report/OS-017-report.md`) mostrou confiança `0.7644` para o fixture "legível" (`clear_text_pdf.pdf`) — **abaixo** do threshold `0.85` reaproveitado da decisão #9. O texto foi lido corretamente (`"THE QUICK BROWN FOX JUMPS OVER 13 LAZY De"`), mas uma região específica ("De", provavelmente leitura parcial de uma palavra final) teve confiança `0.49`, puxando a média das 3 regiões para baixo. Aplicado literalmente, o threshold atual faria esse texto legível cair desnecessariamente para `CloudOCRFallback`. Fixture "ilegível" (`unreadable_text_pdf.pdf`) teve confiança real `~0.0000152` (não exatamente `0.0`, mas na prática equivalente — ainda bem abaixo do threshold). Este agente não decidiu um novo threshold (não é decisão de arquitetura de agente de execução, ver `AGENTS.md` seção 1) — recomendação registrada no relatório para o dono do projeto avaliar antes de ligar `EasyOCRExtractor` em `core/pipeline.py`.
- **Resolvido (OS-034): áudio truncado silenciosamente em idiomas não-ingleses.** `KPipeline.__call__` trata inglês e demais idiomas por caminhos diferentes: inglês usa `en_tokenize()`, que divide respeitando o limite de 510 fonemas; es/fr/hi/it/pt usam `EspeakG2P` e só dividem em `[.!?]` com alvo de 400 caracteres — se **uma única frase** passar disso, o código faz `ps = ps[:510]` e segue, emitindo só um `logger.warning`. Medido neste ambiente: português rende **1,19 fonemas por caractere** (inglês, 0,88), então ~510 fonemas ≈ **430 caracteres**; uma frase de 557 caracteres perdia 141 fonemas, com o áudio terminando no meio da palavra. Atingia texto comum (frase acadêmica longa, ou trecho em que o OCR perdeu a pontuação). **Cuidado (continua valendo): baixar `DEFAULT_MAX_CHARS` não corrigia** — `chunk_text()` nunca corta uma sentença ao meio (contrato desde a OS-008). A OS-034 corrigiu no `KokoroSpeaker`: mede com `g2p` e divide por fronteira natural (nunca no meio de palavra) antes de sintetizar, para todos os idiomas via espeak; verificado empiricamente com o Kokoro real (frase de 557 caracteres: 24,15s → 34,55s, +43% de duração, texto completo). O comentário desatualizado de `processing/chunker.py` foi corrigido.
- **Em aberto, sem solução barata (achado 2026-08-05): qualidade/sotaque do português é limitação inerente do Kokoro.** O `lang_code` está correto (`LANG_CODES['p'] == 'pt-br'`), mas a fonemização usa **espeak-ng** (motor baseado em regras), enquanto o inglês usa um G2P com léxico (`misaki[en]`). O resultado é aproximado — "segurança" vira `sˌeɡuɾˈɐ̃ŋsæ`, terminando em `æ`, vogal que não existe em português —, o que o dono do projeto percebeu como "mistura com português de Portugal". **Não há parâmetro que corrija**; o caminho real seria um `Speaker` alternativo só para esse idioma (possível pela arquitetura de plugins, decisão #1), provavelmente TTS pago — decisão do dono, ver `ARQUITETURA.md` seção 6. A OS-034 corrige o truncamento, **não** esta limitação.
- **Resolvido (OS-036):** OS-027 produz estrutura inútil em livros com TOC aninhado.** Três defeitos, medidos com o "Arquitetura Limpa" (446 páginas, 263 entradas de TOC): (a) a heurística "só nível 1" devolve apenas o front matter, porque a estrutura real do livro está nos níveis 2–5; (b) o último capítulo detectado herda `end_page = total_pages`, então "Sobre o Autor" virou 415 páginas e 320 chunks — o leitor chega em "Capítulo 4 de 4" no trecho 18 de 559 e perde toda a navegação dali em diante; (c) as páginas anteriores ao primeiro item do TOC (1–17, com texto real) **nunca entram em nenhum capítulo e não são sintetizadas** — perda silenciosa de conteúdo. Livros já processados precisam ser reenviados depois da correção. **Corrigido na OS-036:** o nível do TOC passa a ser escolhido por cobertura (o mais raso cujo maior capítulo caiba em 25% do livro), capítulos desproporcionais são subdivididos e as páginas órfãs iniciais entram no primeiro capítulo — medido no mesmo livro: 35 capítulos, maior com 17%, cobertura desde a página 1.
- **Resolvido (OS-035):** quebras de linha do PDF viravam pausas audíveis no meio da frase. `KPipeline.__call__` usa `split_pattern=r'\n+'`, então cada `\n` herdado do PDF vira um segmento de síntese separado. O `cleaner.py` corrige hifenização mas não junta linhas que continuam a mesma frase. Medido num parágrafo real: **5 segmentos onde deveria haver 1**. Como o PDF quebra linha a cada ~80 caracteres, um chunk de 1000 carrega ~12 pausas espúrias. Era a causa mais provável da "narração picotada" relatada. **Corrigido na OS-035** (`_join_wrapped_lines()` no cleaner): o mesmo parágrafo passou a produzir 1 segmento, com integridade de texto verificada.
- Threshold `0.85` (decisão #9) é uma margem de segurança, não um ponto fino validado empiricamente — as fixtures do spike não cobriram degradação intermediária. Se `TesseractOCR` em produção mostrar falsos positivos/negativos de fallback, revisitar com mais dados.

## 7. Como este arquivo deve ser mantido

- Atualizar a seção 2 (status atual) e 4 (componentes) a cada OS concluída.
- Mover item do backlog (seção 5) para "implementado" só depois que o DoD da OS foi cumprido, não quando o código foi só escrito.
- Nunca editar o log de decisões (seção 3) retroativamente — só adicionar.

## 8. Direção de produto: multitenancy e camada de contas (a redigir como OS)

Registrado em 2026-08-06, a partir da conversa com o dono do projeto. **Nada disso está implementado** — é a intenção declarada para a próxima fase, guardada aqui porque o contexto da conversa se perde entre sessões.

**Motivação:** hoje a aplicação é single-user. Os três níveis existem na arquitetura (plugins + flags), mas não há usuário, conta nem plano — o nível é definido por `config.yaml` (global, exige editar arquivo e reiniciar o worker) mais uma flag por livro. Para virar produto vendável isso precisa de uma camada acima.

**Intenção do dono, em palavras dele (a deliberar, não decidido):**

- **Domínio separado para um usuário master**, com controle dos usuários da aplicação (painel administrativo).
- **Aplicação multitenant**, com o máximo de isolamento razoável.
- Abordagem considerada: **Shared Database + RLS** (row-level security).
- **JWT com claim de tenant** para autenticação.
- **Resolução do tenant em middleware**, verificando logo na entrada da requisição.
- **Verificação de pertencimento** (o recurso pedido é mesmo daquele tenant).
- **Isolamento de arquivos** — hoje `uploads/` e `storage/audio/{book_id}/` são globais e sem qualquer noção de dono.
- **Blacklist de JWT** para revogação.

**Consequências técnicas já visíveis, para quem for redigir a OS:**

1. **Banco.** RLS não existe no SQLite. Essa direção implica reabrir a decisão #4 (SQLite → Postgres), que sempre esteve marcada como "migração futura" e nunca foi decidida.
2. **Armazenamento de arquivos.** `uploads/` e `storage/audio/` não têm dono hoje; qualquer isolamento real precisa de caminho por tenant e de verificação no endpoint que serve o áudio.
3. **Plano vs. flag por livro.** Hoje `normalize_text` é por livro. Com plano por conta, é preciso decidir se o plano **restringe** o que o livro pode pedir (provável) e onde essa checagem mora.
4. **`config.yaml` é global.** Com múltiplos tenants em planos diferentes, a escolha de `speaker`/`normalizer` não pode mais ser um arquivo único — vira atributo do plano/tenant.
5. **Chaves de API.** Hoje há uma chave por instalação, em variável de ambiente. Com multitenancy é preciso decidir se o custo é do provedor (uma chave, rateio por uso) ou de cada cliente (chave por tenant).
